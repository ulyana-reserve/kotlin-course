Функция для строковых аргументов

fun example1(arg: String) {
    // твой код и вывод результата через println()
}
---------------------------------------------------------------
Функция для целочисленных аргументов

fun example1(arg: Int) {
    // твой код и вывод результата через println()
}
---------------------------------------------------------------
Функция для двух аргументов

fun example1(arg1: Int, arg2: Char) {
    // твой код и вывод результата через println()
}

----------------------------------------------------------------
Вызвать функции для проверки можно из функции main() которую можно запускать на исполнение

fun main() {
    example1("argument")
    example2(345)
}
